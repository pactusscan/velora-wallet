<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Never backup wallet data — user must do explicit encrypted export -->
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
